# Employee Records App

A small Node.js app for managing employee records with a simple HTML form and JSON-backed storage.

## Run locally

```bash
npm install
npm start
```

The app will be available at http://localhost:3000.
